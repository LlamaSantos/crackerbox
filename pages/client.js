var React = require('react');
var jQuery = require('jquery');

window.jQuery = jQuery;

require('./application.css');
//var Index = require('./index');

// -- Necessary to Prime React with the initial page state.
//React.render(<Index />, document.body);
